- name: FTP Deploy
  uses: SamKirkland/FTP-Deploy-Action@4.3.0
on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: access-5000764687.ud-webspace.de/marincomics.com/profireiniger
        username: a111771
        port: 22
        password: ${{ secrets.ftp_password }}